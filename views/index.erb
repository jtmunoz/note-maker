<div uk-grid="">
	<div class="uk-width-3-5@m">
		<div class="uk-padding-small">
			<h2>All Notes</h2>
			<ul class="uk-list uk-list-divider">
				<% @notes.each do |note| %>
				<li>
					<a href="/note/<%= note.id %>"><%= note.title %></a> | <%= note.pretty_time %>
				</li>
				<% end %>
			</ul>			
		</div>
	</div>
	<div class="uk-width-2-5@m">
		<div class="uk-padding-small">
			<h2>Add A New Note</h2>
			<form method="post" action="/note">
				<label for="title">Title: </label>
				<input class="uk-input" type="text" placeholder="Note Title" name="post[title]">
				<label for="body">Body: </label>
				<input class="uk-input" type="text" placeholder="Note Body" name="post[body]">
				<input class="uk-button uk-button-primary uk-margin-top" type ="submit" value="Add Note">
			</form>					
		</div>
	</div>
</div>